export interface adminLogin {
    email: string;
    password: string;
}

export interface AdminSignup {
    name: string;
    email: string;
    role: string;
    adminCode: string;
    password: string;
}